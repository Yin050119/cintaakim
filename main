<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Love Envelope</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom CSS for heart shapes */
        .heart {
            position: absolute;
            width: 40px;
            height: 40px;
            background-color: #ef4444; /* Red-500 */
            transform: rotate(-45deg);
            animation: float 4s ease-in-out infinite;
        }

        .heart::before,
        .heart::after {
            content: '';
            position: absolute;
            width: 40px;
            height: 40px;
            background-color: #ef4444; /* Red-500 */
            border-radius: 50%;
        }

        .heart::before {
            top: -20px;
            left: 0;
        }

        .heart::after {
            top: 0;
            left: 20px;
        }

        /* Specific heart colors for the button and envelope flap */
        .heart-pink {
            background-color: #f472b6; /* Pink-400 */
        }
        .heart-pink::before,
        .heart-pink::after {
            background-color: #f472b6; /* Pink-400 */
        }

        /* Animation for floating hearts */
        @keyframes float {
            0% { transform: translate(0, 0) rotate(-45deg); }
            50% { transform: translate(0, -15px) rotate(-45deg); }
            100% { transform: translate(0, 0) rotate(-45deg); }
        }

        /* Envelope flap styling */
        .envelope-flap {
            width: 0;
            height: 0;
            border-left: 150px solid transparent;
            border-right: 150px solid transparent;
            border-bottom: 100px solid #fbcfe8; /* Pink-200 */
            position: absolute;
            top: -100px; /* Position above the main envelope body */
            left: 50%;
            transform: translateX(-50%);
            z-index: 10;
            border-radius: 0 0 10px 10px; /* Rounded bottom corners */
        }

        /* Envelope body styling */
        .envelope-body {
            background-color: #fbcfe8; /* Pink-200 */
            width: 300px;
            height: 200px;
            border-radius: 10px;
            position: relative;
            box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
            overflow: hidden; /* Hide overflowing card */
        }

        /* Card inside envelope */
        .envelope-card {
            background-color: white;
            width: 260px;
            height: 160px;
            position: absolute;
            top: 20px;
            left: 20px;
            border-radius: 8px;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.05);
            z-index: 20;
        }

        /* Button styling */
        .heart-button {
            position: absolute;
            bottom: -20px; /* Adjust to place it on the flap */
            left: 50%;
            transform: translateX(-50%) rotate(-45deg);
            width: 50px;
            height: 50px;
            background-color: #ec4899; /* Pink-600 */
            border: none;
            cursor: pointer;
            border-radius: 0; /* Reset default button border-radius */
            z-index: 30; /* Ensure it's above other elements */
            transition: transform 0.2s ease-in-out;
        }

        .heart-button::before,
        .heart-button::after {
            content: '';
            position: absolute;
            width: 50px;
            height: 50px;
            background-color: #ec4899; /* Pink-600 */
            border-radius: 50%;
        }

        .heart-button::before {
            top: -25px;
            left: 0;
        }

        .heart-button::after {
            top: 0;
            left: 25px;
        }

        .heart-button:hover {
            transform: translateX(-50%) rotate(-45deg) scale(1.1);
        }

        /* Ensure Inter font is used */
        body {
            font-family: "Inter", sans-serif;
        }
    </style>
</head>
<body class="bg-pink-100 flex items-center justify-center min-h-screen p-4 overflow-hidden">
    <div class="relative w-full max-w-md flex flex-col items-center justify-center">
        <!-- Floating Hearts -->
        <div class="heart" style="top: 10%; left: 10%; animation-delay: 0s;"></div>
        <div class="heart" style="top: 30%; left: 80%; animation-delay: 1s; width: 30px; height: 30px;"></div>
        <div class="heart" style="top: 60%; left: 15%; animation-delay: 2s; background-color: #f87171;"></div>
        <div class="heart" style="top: 80%; left: 60%; animation-delay: 0.5s; width: 35px; height: 35px;"></div>
        <div class="heart" style="top: 20%; left: 50%; animation-delay: 1.5s; background-color: #fca5a5;"></div>

        <!-- Envelope Container -->
        <div class="relative mt-20 mb-20">
            <!-- Envelope Flap -->
            <div class="envelope-flap"></div>

            <!-- Envelope Body -->
            <div class="envelope-body">
                <!-- Card inside the envelope -->
                <div class="envelope-card"></div>
            </div>

            <!-- Heart Button on the flap -->
            <button class="heart-button" onclick="localStorage.setItem('playMusicOnLoad', 'true'); window.location.href = 'index.html';"></button>
        </div>
    </div>

    <script>
        // Optional: You can add more JavaScript for dynamic interactions here.
        // For example, to make the card slide out on button click.
    </script>
</body>
</html>
